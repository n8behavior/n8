[package]
name = "n8"
version = "0.1.0"
edition = "2024"
description = "Intrinsic isolation for self-containerizing programs"
license = "MIT"
repository = "https://github.com/n8behavior/n8"

[dependencies]
nix = { version = "0.30.1", features = ["sched", "process", "mount", "user"] }
